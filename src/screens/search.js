import { Text, View, SafeAreaView } from "react-native";
import React, { Component } from "react";

export default class search extends Component {
  render() {
    return (
      <SafeAreaView
        style={{ backgroundColor: "rgba(135,26,235,0.2)", flex: 1 }}
      ></SafeAreaView>
    );
  }
}